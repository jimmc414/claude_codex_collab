---
description: Sync with GitHub - pull, then push
---

Sync your local repository with GitHub (pull latest, then push your changes):

!git pull && git add . && git commit -m "Sync changes" && git push

Or with a custom message:
!git pull && git add . && git commit -m "Your message here" && git push

For just pulling latest:
!git pull
